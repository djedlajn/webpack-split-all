import * as lodash from 'lodash'
import react from 'react'
import vue from 'vue'

console.log('OVO JE OK')

const sum = (a, b) => a + b

sum(sum(a + b), lodash.add(20, 20))

vue.config = {}

react.cloneElement()
